spring:
  application:
    name: first-spring-boot
  datasource:
      # 数据库连接地址
      url: jdbc:mysql://localhost:3306/takeway_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
      # 数据库驱动
      driver-class-name: com.mysql.cj.jdbc.Driver
      # 本地 MySQL 用户名
      username: root
      password: 200695zcy
  data:
    redis:
      host: localhost
      port: 6379
      database: 0